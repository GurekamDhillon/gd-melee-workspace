# Recon — session state after Claude session-max death (2026-09-20)

Read-only recon dumped as I go. No project files were modified by recon. This file is a scratch
log for whoever resumes the session; the canonical docs remain `docs/HANDOFF.md` and
`docs/NEXT-SESSION.md`.

## Repository state (verified)

- `master` = `0b36aeb` "docs: rewrite the handoff's gaps and NEXT-SESSION for an 86-commit day".
  Working tree clean except **untracked files only** — nothing modified, nothing stashed,
  no dangling branches. `git log --all` shows nothing stranded.
- The `.claude/worktrees/agent-*` (7 of them) are all **prunable**; their git metadata paths are
  broken from /mnt/c but `git worktree list` marks them so. Nothing to recover there.
- `worktrees/*` (16 dirs: statics, stages, fighters-akaneia, ...) are independent clones with
  broken gitdir metadata (`fatal: not a git repository ... C:/.../melee/.git/worktrees/<name>`),
  but all show clean working trees. Pre-existing condition; not caused by the death.

## THE in-flight thread at death: `tools/port/agent_new.sh` verification

The last activity before the session died (from pasted context):

1. Ran `bash tools/port/agent_new.sh wtcheck` from the main tree. Output: worktree + build root
   created, **998 objects / 25 shimobj hardlinked, "includes 2 generated .inc copied"** — then the
   **mtime step printed nothing and the command hit a 10-minute timeout**.
2. GD diagnosed **two bugs in the patch**: (a) `find` aborted on a missing `include/`
   directory; (b) the backdating anchored to the **newest** object instead of the **oldest**,
   so the checkout still looked stale ("otherwise sources cts").
3. "Fixing:" — then the session died.

**Status of those fixes: they ARE in HEAD**, committed as `4962bfb` "tools: agent_new.sh fixes
the three things a fresh worktree gets wrong", immediately before the handoff rewrite commit.

- Bug (a) fixed: `agent_new.sh:80` guards `if [ -d "$inc_src" ]` before the find/cp.
- Bug (b) fixed: `agent_new.sh:88-107` anchors to `ls -1tr ... | head -1` (oldest) and
  backdates `src pc extern include` to `oldest - 60s`.

**What is NOT confirmed:** the post-fix `wtcheck` re-run. The observed run was the pre-fix one
(the fragment ends at "Fixing:"). `worktrees/wtcheck` and `_build/agents/wtcheck` **do not
exist**, so either the re-verification never ran or it ran and was cleaned up. **TODO on resume:
run `agent_new.sh wtcheck` again and confirm the `mtimes` line prints and it completes well
under 10 min.** The 10-min hang is suspicious: the pre-fix `find ... -exec touch` over the
whole worktree on drvfs may simply be slow — verify, don't assume.

## Other fresh artifacts (Sep 20)

- `_build/agents/trophy` exists (mtime 13:23) — build root for a trophy agent not in the
  HANDOFF's five-agent table. Has `masstest/`, `melee_link_objects.rsp`, `melee-pc.exe`,
  `melee-pc.map`, `runs/`.
- `_research/trophies.md` is the **most recently modified file in the repo** (14:02:10, same
  minute the session died) — read it; it is the freshest thinking, presumably the
  `TY_TROPHY_COUNT` / save-format analysis.
- `_build/agents/menutex` also exists (not in HANDOFF's five).
- Root `_build/` has a bloated pile of pad/capture scripts and captures (pad_*.txt,
  stage_capture*, sonic_capture, stick_capture) plus run logs; all untracked.

## Open thread at death (todo snapshot from the fragment — 19 done, 1 in progress, 4 open)

- IN PROGRESS: **Chase the remaining added-stage failures** (`ext:302` HSD heap exhaust at
  ~frame 273, `ext:306` ucrtbase fault, ACE internal 100 degraded). This is item #1 in
  NEXT-SESSION "Do these first".
- OPEN: Rewrite handoff + NEXT-SESSION — **actually DONE** (commit `0b36aeb`).
- OPEN: rollback / bridge / perf (#10)
- OPEN: m-ex trophies 293..341 save-format decision (#22)
- OPEN: loading screen on the texture path (#23)

Writing TBD — first section appended at the start of upcoming turns.
## Round 2 — sweep status, last stage-chase run, patches, texture path (2026-09-20 ~14:05)

### Sweep summary (`_build/crashlogs/summary.json`, mtime today, 17 tags)

Sampled at 14:05 — the suite that produces 70/70 headless per HANDOFF/NEXT-SESSION:

- **OK (13):** diddy/charizard/lucas/sonic/dedede/tails/fox-c45/purin-c56/gw-c45/spread/refuse-99/refuse-9/vanilla/ace-boot
- **PROBLEM (3):** `wolf` AV at `0x10044D7D` (Akaneia), `kirby-dk` AV at `0x1026E840` (Akaneia), `metalsonic` PROBLEM with **empty fault** (ACE — harness under-report bug §2 in NEXT-SESSION, now fixed to parse logs; this one looks like the old class).
- The `wolf` and `kirby-dk` RVAs are new vs HANDOFF's "all five Akaneia fighters green" era — regression or a different suite slice (this file lists `fox-c45` etc. as c45/c56 costume variants, not just the base 7). Not investigated further in recon.

### Last stage-chase run: `_build/runs/hp302` (mtime 2026-09-20 14:01:29)

- Name suggests `ext:302` (GrGh.dat) — NEXT-SESSION's "most interesting bug" (HSD heap exhaust at ~frame 273 *after rendering*).
- **Actual log is NOT that bug.** It dies at `gw: FATAL ACCESS_VIOLATION read 0x00000004 at 0x1037D3F9 (rva 0x1037D3F9)` **during boot**, right after `7 m-ex fighter kinds from MxDt.dat (kinds 33..39)` and Kirby hat lines. Stack: `1037D3F9 -> 10334325 -> 1034BD4E -> 10112714`. No DIAG prim/dlist, no scene — never reached the game loop. Suggests a boot-time deref of a null-adjacent pointer in the fighter/hat init path, not the heap exhaust. Either hp302 is a *different* repro (vanilla fighter + added stage? vs added fighter + vanilla stage?) or the binary in that run predates the heap fix. Check `rva 0x1037D3F9` via `mapsym.sh` on resume.
- Card/run have `mods: sonic (19 new paths)` from a `MELEE_MODS`-style parent — matches PORT_BOOTSTRAP "build a mod folder from your own discs" (commit cd09bf8).

### Patch discrepancy — trophies doc cites a patch that isn't there

- `trophies.md` §6 says overlay fix is "left as `_build/patches/overlay-count-dlists.patch`" (shim_gx.c counts `gw_gx_prim_count` but trophy collection room draws via dlists only, so `prim=0 dlist=129` keeps LOADING panel up).
- **On disk `_build/patches/` contains only `grstages-exec-trap-arm.patch`** (commit 12:14 today: arm execute trap for stage blobs, not just fighters — `gw_mex_any_installed` guard). The overlay patch **does not exist** — either never written, written elsewhere, or cleaned. If you resume trophies, rewrite it from the doc's one-liner.

### Texture path — menutex / vfy runs

- `_build/runs/menutex*` (off/rgb5a3/rgba8) each have `melee-pc.log` + `frame_17s.png` + pipeline/dawn caches, mtime 13:42-13:45 today. These are the PNG→GX→HSD verification runs from NEXT-SESSION's "PNG→GX texture→drawn works, verified on screen".
- `_build/runs/vfy-tex` (13:48) and `vfy-overlay` (13:55) look like follow-up verifications of that path and the loading overlay — not inspected beyond mtimes.
- Pads: `_build/suite/` at 10:53 holds `ace-ck34-*` logs/pngs (Wolf on ACE, all move groups) — matches `_build/masstest/files.txt` = **988 TUs**, same as HANDOFF's "~980 translation units". The sweep's named faulting runs now use per-run `dawn_cache` + empty `card` isolation per commit 2ed9183/6094c36.


## Round 3 — plans, untracked research, trophy agent (2026-09-20 ~14:10)

### `.omo` orchestration state

- `boulder.json` active_work_id = `sonic-port-completion`, active_plan = `.omo/plans/sonic-port-completion.md`, worktree_path = null. Session ids contain `opencode:sonic-port-completion` — the Boulder was never switched after that milestone.
- `sonic-port-completion.md` is a **stale plan** (Sonic double-jump STACK_OVERFLOW + neutral-B NULL deref). Todos 1-2 marked [x], 3-10 still open, but HANDOFF/NEXT-SESSION say Sonic is *verified working* (spring up-B verified, Movelogic soak done) and the 70/70 sweep was on 988 TUs. The plan's baseline `108fa831e` predates current HEAD `0b36aeb` by ~60 commits. **Do not resume this plan — HANDOFF/NEXT-SESSION are authoritative.** Archive or bump Boulder to a new work id before starting new work.
- `melee-pc-boot-unblock.md` is even older (Sept 13, D3D11 flicker, boot-to-title) — fully superseded. Kept for audit trail only.

### Untracked `_research/*.md` — active threads NOT in HANDOFF's reading list

All four are untracked (git status `??`) and research-only (no licence violation — spec only):

- `mex-sound-banks.md` (2026-09-19, COMPLETE): what Sonic needs to play his own SSM — 78 sound banks from mexData, stage audio 96 rows. Related to `tools/mex_port/dump_ssm.py` etc. now in `_build`.
- `mex-stock-icons.md` (2026-09-19): HUD/1P CSS stock icons for Sonic — byte-checked against Akaneia's `codes.gct`.
- `rollback-netcode.md` (2026-09-19): rollback research/design (distinct from committed `rollback-port-design.md` 2026-09-19 20:36 which lives on `agent/rollback` per HANDOFF). Both tagged VERIFIED/INFERRED.
- `sonic-clone-boot.md` (WIP): Ft_Kind_Sonic boot via `ftMapping_list[ChKind_Popo]` dev remap + `MELEE_TARGET_TEST=32` — the earlier Sonic bring-up thread, now merged.

These match the `tools/mex_port/dump_*.py` untracked scripts (dump_fighters/gct/mex_slots/ssm/stc_icns) — the dump tooling that feeds the research. None were referenced in the last 86-commit day's merges; they are **carry-over research** to triage before deleting or promoting to tracked docs.

### Trophy agent build root `_build/agents/trophy`

- Exists (mtime 13:23 today), `8203264` byte `melee-pc.exe`, `melee-pc.map`, `masstest/` + `melee_link_objects.rsp` + `runs/` — but **no `melee-pc.log`** at root (maybe logs live under `runs/trophy*` or were never run). Binary is ~700KB smaller than `hp302`'s 8214528 — different link (different objects/bridge). Likely the agent that produced `trophies.md` at 14:02 (gallery/lottery/collection verified). Check `runs/tygal*`/`tycoll*`/`tylot*` for its evidence — not inspected in recon (read-only limit).

### What remains for a resuming session (ranked)

1. **Verify `agent_new.sh` post-fix** — `wtcheck` re-run must print `mtimes … backdated` and finish <30s on drvfs; if it still hangs, the `find -exec touch` needs `+` batching or a WSL-native path note.
2. **Stage gap #1** — hp302 is NOT the described heap exhaust; re-chase `ext:302` (HSD heap at frame ~273) with `MELEE_SCENE="mode=training;p1=fox;stage=ext:302"` and heap trace; `ext:306` ucrtbase fault is a host/guest pointer mirror gap in `gw_ppc_call`.
3. **Trophies gap #2** — trophies *work* but m-ex ids 293..341 are invisible by design; raising `TY_TROPHY_COUNT` is a save-format break — decide before touching `gm/types.h`/`ty/types.h`.
4. **Texture path is green** — menutex/vfy runs prove PNG→GX→HSD; loading screen (#23) is now unblocked.
5. **Housekeeping** — `_build/patches/overlay-count-dlists.patch` missing; `sonic-port-completion` Boulder stale; 17 `_build/pad_*.txt` + capture dirs are untracked noise from soaks.


## Round 4 — `wtcheck` 10m hang deep dive (2026-09-20 ~14:15) — read-only verification

**Context:** `bash tools/port/agent_new.sh wtcheck` printed `includes 2 … copied` then hit the 10m timeout with no `mtimes … backdated` line. Pasted note says two bugs in the patch were being fixed: `find` abort on missing `include/` and newest-vs-oldest anchor.

**Current file verification (HEAD = 4962bfb, matches disk):**

- `include` guard **fixed**: `portlib.sh` + `agent_new.sh:80` `if [ -d "$inc_src" ]` and `for d in src pc extern include; do [ -d "$worktree/$d" ] || continue` — dry-run in Git Bash shows `include: missing (skipped by guard)` with zero error, so `find` no longer aborts. Before this guard, `find "$worktree/include" …` on a missing dir would exit non-zero and, with `set -euo pipefail`, kill the script.
- Oldest-anchor **fixed**: `agent_new.sh:88-107` anchors to `ls -1tr … | head -1` (oldest) and stamps 60s before it. Dry-run in **both** WSL and Git Bash on the live baseline (998 objs) yields `oldest_obj=…/src_melee_db_dballoc.c.obj`, `obj_epoch=1789882200`, `stamp=202609192229.00` correctly — the mtimes echo would be `mtimes 3 source trees backdated to 202609192229.00`. The old `ls -1t | head -1` (newest) still produced a stamp but left most sources looking stale, defeating the purpose.
- Performance **not the hang**: `find` for the `*.c/*.h/*.cpp/*.inc` set on the proxy `melee/src` is `2158 files` in **0.035s (Git Bash)** / 0.093s (WSL) for src alone; pc 44, extern 238, include skipped → ~2440 files total. With `-exec touch -t "$stamp" {} +` batching, the real loop should be <1s on this FS. The observed 10m hang was against the **pre-fix** file — likely the unguarded `find` on missing `include` combined with `set -e` pipefail stalling, or an older `find … -exec touch … \;` (per-file) variant. Current `+` batching resolves it.

**What remains unverified live:** `worktrees/wtcheck` and `_build/agents/wtcheck` were removed (no trace on disk), so the post-fix `mtimes … backdated` line has **never been observed to print in a live run**. Next `wtcheck` creation should finish well under 10s and be the proof.


## Round 5 — added-stage failures (in-progress task) — read-only log forensics

**NEXT-SESSION says:** `ext:302` (GrGh.dat) exhausts HSD heap at ~frame 273 *after rendering* (most interesting — over-alloc somewhere); `ext:306` (GrGc.dat, 34,844 bytes) faults in `ucrtbase` via host/guest pointer mirror; ACE internal 100 (GrKcs.dat) degraded (no bridge for `StageCallbacks[]` at `0x803E6328`).

**What survives on disk:**

- `_build/runs/gr-302/` (no scene): boot to `GM_BOOT` only, `prim=0 dlist=0`, `synth: bank …` — **never entered the stage**. Not the heap chase; likely a default-run or harness check. No crashlog.
- `_build/runs/hp302/` (14:01 today, likely the heap chase): early `FATAL ACCESS_VIOLATION read 0x00000004 at 0x1037D3F9` right after `7 m-ex fighter kinds …` and Kirby hats, before `scene:` or `grfunction:` — a boot-time NULL-adjacent deref (`rva 0x1037D3F9`), not the described heap exhaust. Either the binary in that run predates the stage fix, the stage was not selected, or the heap exhaust only repros after the stage actually renders (this run died before `grfunction:`). Needs `mapsym.sh 0x1037D3F9` and a re-run with `MELEE_SCENE="mode=training;p1=fox;stage=ext:302"`.
- `_build/runs/art-306/` (**the real ext:306 chase**, Akaneia, 206 lines): `MELEE_SCENE="mode=training;p1=fox/c0/hu;stage=ext:306"` → `grfunction: /GrGc.dat internal 89 installed: code 0x811560E0..0x8115E8FC 34844 bytes, 8/8 overloads` + 9 thunks `0x811564F4..0x81156794` correctly bound. Then **`interp: unresolved guest call target 0x80353424 (no bridge entry)` + `ppc: resolver returned NULL`** and `gw: FATAL` at `ntdll+0x7A0FC`. This matches NEXT-SESSION's description (pointer crossing host/guest that `gw_ppc_call` mirror doesn't cover) — the concrete missing bridge is `0x80353424` in this stage's blob.
- No surviving log shows the `~frame 273 HSD heap NULL` for 302; `grep -r "HSD_MemAlloc\|heap\|GrGh" _build/runs/` finds only `a1`'s heap table shape test, not a 302 run. The evidence for that bug is either in an un-captured log (overwritten `melee-pc.log`) or only in the NEXT-SESSION author's memory — re-produce it.

**Implication for Claude:** 302 needs a fresh instrumented run with heap trace (`MELEE_HEAP_TRACE=1`) to catch the `HSD_MemAlloc -> NULL` at frame ~273; 306 needs a bridge entry for `0x80353424` (check `bridge-signatures.md` / `gw_mex_sigs_gen.inc` for why it's missing — doubles/by-value struct?).


## Round 6 — remaining 4 open tasks from that 24-task snapshot (read-only)

**Snapshot at death:** `24 tasks (19 done, 1 in progress, 4 open)` — the in-progress was the stage chase (Round 5). The 4 open were:

### 1. Rewrite the handoff and NEXT-SESSION — DONE
`0b36aeb docs: rewrite … for an 86-commit day` already landed. `HANDOFF.md` §1 gaps rewritten 2026-09-20, `NEXT-SESSION.md` written 2026-09-20 with "three ways this tree lies" and `Do these first: #20 stages, #23 loading screen, #22 trophies, #10 rollback/bridge/perf`. No work left — this open checkbox in the pasted snapshot is stale.

### 2. Remaining engine gaps: rollback, bridge, perf (#10)
- **Rollback:** `_research/rollback-port-design.md` (design only, 2026-09-19 20:36, 44KB) + `_research/rollback-netcode.md` (untracked, research). Numbers are **[M] measured** from `melee-pc.map` 2026-09-19: 4,812 symbols = 1,548,860 bytes in `.data/.bss` to snapshot; timings from 4 small clang-built test programs on this machine. No code under `src/` or `pc/platform/` — branch `agent/rollback` per HANDOFF does not exist as a local branch (only `origin-ws/master` remote). Next step is not code — it's perf baseline (nobody has profiled yet).
- **Bridge:** `_research/bridge-signatures.md` + `_build/gw_mex_sigs_gen.inc` (+ `.report.txt`) = generated but **not yet wired into the build**. Audit `bridge-signatures-audit.txt`: 101 blob targets, 96 resolved / 5 unresolved; total scope 110 → 104/6. Float gaps: 22. The one `NOT-COVERED` that matters is `0x803228C0 __cvt_fp2unsigned` (double param, bridge marshals 4-byte slots only). Doubles/by-value structs still unsupported (472 symbols keep integer-default). The `0x80353424` miss in `art-306` is in that class — check if it's a double/struct sig.
- **Perf:** No baseline at all. HANDOFF §1 gap 3 says `gw_ppc_static_native` gates bridge lookup at `0x80300000` while main heap starts `~0x806A00000`, so every interpreted fighter-struct access pays ~15-probe binary search — raising the gate is a free speed-up.

### 3. Show m-ex trophies 293..341 — needs save-format decision (#22)
`trophies.md` (2026-09-20, freshest file) proves gallery/collection/lottery now run (lottery was faulting at `_gw_Toy_80306D70+0x7E` reading `0x9483D34C`, fixed via alias-view). Verified on `tygal`/`tycoll`/`tylot7` captures. **But** `TyDataf.dat` is 293 vanilla / 342 Akaneia & ACE (ACE adds none), and `TY_TROPHY_COUNT=293` bounds every loop/stack `trophy_flags[293]` + `times[293]` + 2× `[(293+31)/32]` bitfields + `HSD_MemAlloc` + ~20 loops + two 293-entry stack arrays. Raising it is a **save-format break** — the doc logs `*** OUT OF RANGE ***` for ids >292. The `tylot7` "GOT IT!" popup and `tycoll_ace` (ACE showing m-ex trophies) already work within the 293 cap; the *invisible* 293..341 are deliberately ignored.

### 4. Build the loading screen on the texture path (#23)
Now **unblocked**. `menutex` proves `pc/tools/png2gx.py` → `gw_GxTex_*` → `HSD_SObj` draw works: `_build/runs/menutex/panel_bg.gxtex 1024x512 fmt9` → quad, and `vfy-tex` shows `aur drawcalls=296 merged=102 verts=20856 prim=16 dlist=254` — healthy rendering. The blocker was the texture path not existing; now trophies' `overlay-count-dlists.patch` is the same one-liner pattern (`gw_gx_prim_count` vs `dlist`).


## Round 7 — the 19 done (so you don't redo them) — from git log HEAD~25..HEAD

The snapshot's "19 done" are the 86-commit day's merges before the death. In commit order (oldest → newest of the top 25):

- `c2f5098` gate build on bridge ABI audit + `57c386a` census native returns + `63053aa` build_gwtool portability — bridge plumbing.
- `90599bd` next-session start point / play-test queue
- `479e9b6` drive every added fighter through moveset + every added stage + `436947e` -Visible on main desktop + `de1ac72` split sweep by move type — the suite that now gives 70/70 / 81/109 stages.
- `51ce19b` bisect failed group vs 4 fighters + `d0d2b35` rebuild stale shim + `2ed9183` snapshot build once + `6094c36` sandbox leftovers — sweep harness hardening.
- `43d17a5` bridge statics shared-name fix (9032 addrs discarded) + `7daec11` merge — the `__cvt` double bug class.
- `055a1d0` fault from log not console (the 150→52 under-report) + `f1b8e24` refuse 2nd sweep + `e9ffd74` 4-at-a-time stages-first + `9b884f3` visible runs + named faults + `5449e4b` guest extents + `4a18e31` volume by PID + `02ce039` rebuild stale GAME TU + `31359cb` quote aurora paths — the "three ways this tree lies" fixes + concurrency fixes (Dawn cache, watchdog, volume, sweep lock).
- `cd09bf8` build mod folder from own discs (so `MELEE_MODS_DIR` parent, not pack itself) + `4962bfb` the `agent_new.sh` three-thing fix (stale TU backdate, `.inc` copy, aurora junction) — the work you were verifying with `wtcheck`.

Evidence trail for the earlier Sonic phase lives under `.omo/evidence/task-*` (double-jump, static-globals, movelogic, etc.) and `.omo/drafts/` — all pre-86-commit, now superseded.


## Round 8 — build health (read-only, 2026-09-20 ~14:55)

- `_build/melee-pc.exe` 8,212,480 bytes @ 13:54 today (map 5,001,915) — **newer than newest obj** `src_melee_mn_mncharsel.c.obj @ 13:47` — link is not stale (the `LNK1104` / stale-TU fix `02ce039` + `build.sh` fixpoint is holding). Oldest obj `src_melee_db_dballoc.c.obj @ 2026-09-19 22:30` is the anchor for `agent_new.sh`'s `stamp=202609192229`.
- `_build/gw_mex_sigs_gen.inc` = 133 lines generated; report: **NOT emitted (keeps integer default)** = 6 entries — `efSync_Spawn` varargs, `__cvt_fp2unsigned` double, `HSD_ForeachAnim` varargs, and three `0x803D7058/88/94` m-ex routines over vanilla `.data` (no symbol to derive from). Those plus the 472 double/by-value-struct keepers are HANDOFF's "remaining bridge gaps" — `0x80353424` from `art-306` is likely in that set.
- `.env` ISO paths are set: `GW_ISO_VANILLA` / `AKANEIA` / `ACE` all `C:/iso/*.iso` — same three NEXT-SESSION and `build.sh`/`run.sh` expect. WSL `ls C:/iso/*.iso` fails due to path style, but Git Bash `ls` earlier proved `C:/iso/Akaneia.iso` and `C:/iso/SSBM ACE Build v2.0.0.iso` exist (they were opened by every `melee-pc.log`).

